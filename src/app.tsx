import * as React from 'react';
import { Container } from '@mui/material';
import MainPage from './Pages/index';

const App: React.FC = () => (
  <Container>
    <MainPage />
  </Container>
);

export default App;
